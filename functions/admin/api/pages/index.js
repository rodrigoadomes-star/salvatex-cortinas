import { json, requireAdmin, clean, slugify, logAdmin } from "../_auth.js";
export async function onRequestGet(context){const a=requireAdmin(context);if(!a.ok)return a.response;const rows=await context.env.DB.prepare(`SELECT * FROM pages WHERE store_id='salvatex' ORDER BY updated_at DESC`).all();return json({ok:true,pages:rows.results||[]})}
export async function onRequestPost(context){const a=requireAdmin(context);if(!a.ok)return a.response;let b={};try{b=await context.request.json()}catch{return json({ok:false},400)}const id=crypto.randomUUID(),now=new Date().toISOString(),title=clean(b.title,240);await context.env.DB.prepare(`INSERT INTO pages(id,store_id,title,slug,content_html,seo_title,seo_description,active,created_at,updated_at) VALUES(?1,'salvatex',?2,?3,?4,?5,?6,?7,?8,?8)`).bind(id,title,slugify(b.slug||title),clean(b.contentHtml,50000),clean(b.seoTitle,240),clean(b.seoDescription,500),b.active===false?0:1,now).run();await logAdmin(context.env.DB,"page_created","page",id,{title});return json({ok:true,id},201)}
