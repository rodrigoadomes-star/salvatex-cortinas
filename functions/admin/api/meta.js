import {requireAdmin,json} from './_auth.js';
async function companyIdForStore(db,storeId){try{const row=await db.prepare(`SELECT company_id FROM platform_company_stores WHERE store_id=?1 LIMIT 1`).bind(storeId).first();return row?.company_id?String(row.company_id):''}catch{return ''}}
export async function onRequestGet(context){const auth=await requireAdmin(context);if(!auth.ok)return auth.response;const companyId=await companyIdForStore(context.env.DB,auth.storeId);if(!companyId)return json({ok:false,code:'COMPANY_NOT_FOUND',message:'Empresa não vinculada à plataforma.'},404);const integration=await context.env.DB.prepare(`SELECT status,external_account_id,external_account_name,token_expires_at,connected_at,last_checked_at,last_error,updated_at,metadata_json FROM platform_integrations WHERE company_id=?1 AND provider='meta' LIMIT 1`).bind(companyId).first().catch(()=>null);const accounts=await context.env.DB.prepare(`SELECT meta_ad_account_id,name,currency,timezone_name,status,is_selected,synced_at,updated_at FROM platform_meta_ad_accounts WHERE company_id=?1 ORDER BY is_selected DESC,name,meta_ad_account_id`).bind(companyId).all().catch(()=>({results:[]}));let metadata={};try{metadata=integration?.metadata_json?JSON.parse(integration.metadata_json):{}}catch{}return json({ok:true,companyId,storeId:auth.storeId,connected:integration?.status==='connected',integration:integration?{status:integration.status,metaUserId:integration.external_account_id||'',metaUserName:integration.external_account_name||'',tokenExpiresAt:integration.token_expires_at||null,connectedAt:integration.connected_at||null,lastCheckedAt:integration.last_checked_at||null,lastError:integration.last_error||null,updatedAt:integration.updated_at||null,adAccountsFound:Number(metadata.ad_accounts_found||0)}:null,adAccounts:(accounts.results||[]).map(row=>({id:String(row.meta_ad_account_id||''),graphId:`act_${String(row.meta_ad_account_id||'')}`,name:String(row.name||''),currency:String(row.currency||''),timezone:String(row.timezone_name||''),status:String(row.status??''),selected:Number(row.is_selected||0)===1,syncedAt:row.synced_at||null,updatedAt:row.updated_at||null}))})}
export async function onRequestPatch(context){const auth=await requireAdmin(context);if(!auth.ok)return auth.response;const companyId=await companyIdForStore(context.env.DB,auth.storeId);if(!companyId)return json({ok:false,code:'COMPANY_NOT_FOUND'},404);let body={};try{body=await context.request.json()}catch{return json({ok:false,message:'JSON inválido.'},400)}const accountId=String(body.accountId||'').trim().replace(/^act_/,'');if(!accountId)return json({ok:false,message:'Conta de anúncios obrigatória.'},400);const account=await context.env.DB.prepare(`SELECT meta_ad_account_id FROM platform_meta_ad_accounts WHERE company_id=?1 AND meta_ad_account_id=?2 LIMIT 1`).bind(companyId,accountId).first();if(!account)return json({ok:false,code:'ACCOUNT_NOT_FOUND',message:'Conta não pertence a esta empresa.'},404);const now=new Date().toISOString();await context.env.DB.batch([context.env.DB.prepare(`UPDATE platform_meta_ad_accounts SET is_selected=0,updated_at=?2 WHERE company_id=?1`).bind(companyId,now),context.env.DB.prepare(`UPDATE platform_meta_ad_accounts SET is_selected=1,updated_at=?3 WHERE company_id=?1 AND meta_ad_account_id=?2`).bind(companyId,accountId,now)]);return json({ok:true,companyId,accountId,updatedAt:now})}
