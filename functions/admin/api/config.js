import { json, requireAdmin, logAdmin } from "./_auth.js";
export async function onRequestGet(context){const a=requireAdmin(context);if(!a.ok)return a.response;const row=await context.env.DB.prepare(`SELECT value_json,updated_at FROM store_configs WHERE store_id='salvatex' AND config_key='site_config'`).first();let config={};try{config=JSON.parse(row?.value_json||'{}')}catch{}return json({ok:true,config,updatedAt:row?.updated_at||null})}
export async function onRequestPut(context){const a=requireAdmin(context);if(!a.ok)return a.response;let b={};try{b=await context.request.json()}catch{return json({ok:false,message:"JSON inválido"},400)}const cfg=b.config&&typeof b.config==='object'?b.config:null;if(!cfg)return json({ok:false,message:"Configuração inválida"},400);const now=new Date().toISOString();await context.env.DB.prepare(`INSERT INTO store_configs(store_id,config_key,value_json,updated_at) VALUES('salvatex','site_config',?1,?2) ON CONFLICT(store_id,config_key) DO UPDATE SET value_json=excluded.value_json,updated_at=excluded.updated_at`).bind(JSON.stringify(cfg),now).run();await logAdmin(context.env.DB,"site_config_updated","store_config","site_config");return json({ok:true,updatedAt:now})}
