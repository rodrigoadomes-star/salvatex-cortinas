import { json, requireAdmin } from "./_auth.js";
function safeStore(s){return String(s||'tenant').toLowerCase().replace(/[^a-z0-9_-]+/g,'-').replace(/^-+|-+$/g,'').slice(0,80)||'tenant'}
export async function onRequestGet(context){const a=await requireAdmin(context);if(!a.ok)return a.response;if(!context.env.MEDIA)return json({ok:false,message:'R2 indisponível.'},503);const storeId=a.storeId,u=new URL(context.request.url),orderId=u.searchParams.get('orderId')||'',key=u.searchParams.get('key')||'',tenantPrefix=`private/orders/${safeStore(storeId)}/${orderId}/`,legacyPrefix=`private/orders/${orderId}/`;if(!key.startsWith(tenantPrefix)&&!(storeId==='salvatex'&&key.startsWith(legacyPrefix)))return json({ok:false,message:'Documento inválido.'},400);const order=await context.env.DB.prepare(`SELECT id FROM orders WHERE id=?1 AND store_id=?2`).bind(orderId,storeId).first();if(!order)return json({ok:false,message:'Pedido não encontrado.'},404);const obj=await context.env.MEDIA.get(key);if(!obj)return json({ok:false,message:'Arquivo não encontrado.'},404);const metadataStore=String(obj.customMetadata?.storeId||'');if(metadataStore&&metadataStore!==storeId)return json({ok:false,message:'Documento não pertence a esta empresa.'},403);return new Response(obj.body,{headers:{'content-type':obj.httpMetadata?.contentType||'application/octet-stream','content-disposition':`attachment; filename="${key.split('/').pop()}"`,'cache-control':'private,no-store'}})}
