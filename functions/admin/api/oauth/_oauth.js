import { json } from "../_auth.js";
export function baseUrl(context){return new URL(context.request.url).origin}
export async function saveState(db,key,state){await db.prepare(`INSERT INTO store_configs(store_id,config_key,value_json,updated_at) VALUES('salvatex',?1,?2,?3) ON CONFLICT(store_id,config_key) DO UPDATE SET value_json=excluded.value_json,updated_at=excluded.updated_at`).bind(key,JSON.stringify({state,expires:Date.now()+10*60*1000}),new Date().toISOString()).run()}
export async function consumeState(db,key,state){const row=await db.prepare(`SELECT value_json FROM store_configs WHERE store_id='salvatex' AND config_key=?1`).bind(key).first();let d={};try{d=JSON.parse(row?.value_json||'{}')}catch{};if(!state||state!==d.state||Date.now()>Number(d.expires||0))return false;await db.prepare(`DELETE FROM store_configs WHERE store_id='salvatex' AND config_key=?1`).bind(key).run();return true}
export async function getMarketing(db){const row=await db.prepare(`SELECT value_json FROM store_configs WHERE store_id='salvatex' AND config_key='marketing_config'`).first();try{return JSON.parse(row?.value_json||'{}')}catch{return {}}}
export async function saveMarketing(db,cfg){await db.prepare(`INSERT INTO store_configs(store_id,config_key,value_json,updated_at) VALUES('salvatex','marketing_config',?1,?2) ON CONFLICT(store_id,config_key) DO UPDATE SET value_json=excluded.value_json,updated_at=excluded.updated_at`).bind(JSON.stringify(cfg),new Date().toISOString()).run()}
export function redirectAdmin(context,status){return Response.redirect(baseUrl(context)+'/admin/'+(status?'?'+status:'')+'#integrations',302)}
