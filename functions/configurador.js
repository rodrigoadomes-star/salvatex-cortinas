class TenantShell{element(el){el.append('<script src="/js/tenant-configurator-shell.js?v=20260820-1"></script>',{html:true})}}
function hostOf(request){try{return new URL(request.url).hostname.toLowerCase()}catch{return''}}
export async function onRequest(context){const host=hostOf(context.request);if(!context.env.ASSETS)return context.next();const u=new URL(context.request.url);u.pathname='/configurador.html';const response=await context.env.ASSETS.fetch(u);if(!host.endsWith('.radzhub.com.br')||host==='salvatex.radzhub.com.br'||host==='radzhub.com.br'||typeof HTMLRewriter==='undefined')return response;const headers=new Headers(response.headers);headers.delete('content-length');headers.set('cache-control','no-store');return new HTMLRewriter().on('body',new TenantShell()).transform(new Response(response.body,{status:response.status,statusText:response.statusText,headers}))}