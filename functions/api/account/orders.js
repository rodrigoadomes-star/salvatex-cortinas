import { json } from "../_lib.js";import { requireCustomer } from "../_customer-auth.js";export async function onRequestGet(context){const a=await requireCustomer(context);if(!a.ok)return a.response;if(!context.env.DB)return json({ok:false,message:'Banco indisponível.'},503);const rows=await context.env.DB.prepare(`SELECT id,order_number,status,stage,total_cents,created_at,updated_at,freight_json FROM orders WHERE store_id='salvatex' AND lower(customer_email)=lower(?1) ORDER BY created_at DESC LIMIT 100`).bind(a.user.email).all();const orders=(rows.results||[]).map(o=>{let f={};try{f=JSON.parse(o.freight_json||'{}')}catch{};return{...o,freight:f}});return json({ok:true,user:a.user,orders})}
