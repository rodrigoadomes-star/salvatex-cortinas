import { requireCustomer } from "../_customer-auth.js";import { json } from "../_lib.js";export async function onRequestGet(context){const a=await requireCustomer(context);if(!a.ok)return a.response;if(!context.env.MEDIA)return json({ok:false,message:'R2 indisponível.'},503);const u=new URL(context.request.url),orderId=u.searchParams.get('orderId')||'',key=u.searchParams.get('key')||'';if(!key.startsWith(`private/orders/${orderId}/`))return json({ok:false,message:'Documento inválido.'},400);const o=await context.env.DB.prepare(`SELECT id FROM orders WHERE id=?1 AND store_id='salvatex' AND (customer_account_id=?2 OR (?3=1 AND lower(customer_email)=lower(?4)))`).bind(orderId,a.user.userId,a.user.emailVerified?1:0,a.user.email).first();if(!o)return json({ok:false,message:'Pedido não encontrado.'},404);const obj=await context.env.MEDIA.get(key);if(!obj)return json({ok:false,message:'Arquivo não encontrado.'},404);const headers=new Headers();headers.set('content-type',obj.httpMetadata?.contentType||'application/octet-stream');headers.set('content-disposition',`attachment; filename="${key.split('/').pop().replace(/\"/g,'')}"`);headers.set('cache-control','private, no-store');return new Response(obj.body,{headers})}

