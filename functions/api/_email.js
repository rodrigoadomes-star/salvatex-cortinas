import {cleanText} from './_lib.js';
export function emailEnabled(env){return Boolean(String(env.RESEND_API_KEY||'').trim()&&String(env.EMAIL_FROM||'').trim())}
function escapeHtml(s){return String(s||'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}
export async function sendSecurityEmail(context,{to,subject,title,message,actionLabel,actionUrl,idempotencyKey}){if(!emailEnabled(context.env))throw new Error('EMAIL_NOT_CONFIGURED');const html=`<!doctype html><html><body style="font-family:Arial,sans-serif;color:#172033"><h1>${escapeHtml(title)}</h1><p>${escapeHtml(message)}</p><p><a href="${escapeHtml(actionUrl)}" style="display:inline-block;padding:12px 18px;background:#111;color:#fff;text-decoration:none;border-radius:8px">${escapeHtml(actionLabel)}</a></p><p>Se você não solicitou esta ação, ignore esta mensagem.</p></body></html>`;const r=await fetch('https://api.resend.com/emails',{method:'POST',headers:{authorization:`Bearer ${context.env.RESEND_API_KEY}`,'content-type':'application/json','user-agent':'Salvatex-Cortinas/1.0','idempotency-key':cleanText(idempotencyKey,200)},body:JSON.stringify({from:String(context.env.EMAIL_FROM),to:[to],subject,html})});if(!r.ok)throw new Error('EMAIL_SEND_FAILED');return r.json()}

