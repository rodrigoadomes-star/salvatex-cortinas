(async function(){const form=document.getElementById('reset-password-form'),out=form.querySelector('.account-feedback'),cfg=await fetch('/api/turnstile-config').then(r=>r.json()).catch(()=>({}));for(let i=0;i<40&&!window.turnstile;i++)await new Promise(r=>setTimeout(r,100));let widget=null;if(cfg.sitekey&&window.turnstile)widget=turnstile.render(form.querySelector('.account-turnstile'),{sitekey:cfg.sitekey});form.onsubmit=async e=>{e.preventDefault();const f=new FormData(form);if(f.get('password')!==f.get('confirm'))return out.textContent='As senhas não coincidem.';const r=await fetch('/api/auth/reset-password',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({token:new URLSearchParams(location.search).get('token'),password:f.get('password'),turnstileToken:widget===null?'':turnstile.getResponse(widget)})}),d=await r.json();out.textContent=d.message;if(r.ok)setTimeout(()=>location.href='minha-conta.html',1500)}})();

