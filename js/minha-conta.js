(function(){const brl=c=>(Number(c||0)/100).toLocaleString('pt-BR',{style:'currency',currency:'BRL'});const esc=s=>String(s||'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));const status=s=>({aguardando_pagamento:'Aguardando pagamento',pago:'Pago',em_producao:'Em produção',pronto:'Pronto',enviado:'Enviado',entregue:'Entregue',cancelado:'Cancelado',reembolsado:'Reembolsado'}[s]||s);async function load(){const s=await fetch('/api/auth/session',{cache:'no-store'}).then(r=>r.json());if(!s.authenticated)return;document.getElementById('account-login').hidden=true;document.getElementById('account-content').hidden=false;const logout=document.getElementById('account-logout');logout.hidden=false;logout.onclick=async()=>{await fetch('/api/auth/logout',{method:'POST'});location.reload()};document.getElementById('account-user').innerHTML=`<strong>${esc(s.user.name||'')}</strong><span>${esc(s.user.email)}</span>`;const d=await fetch('/api/account/orders',{cache:'no-store'}).then(r=>r.json());document.getElementById('account-orders').innerHTML=(d.orders||[]).map(o=>`<article class="account-order"><div><span>Pedido</span><strong>${esc(o.order_number)}</strong><small>${new Date(o.created_at).toLocaleDateString('pt-BR')}</small></div><div><span>Status</span><strong>${esc(status(o.status))}</strong>${o.freight?.trackingCode?`<small>Rastreio: ${esc(o.freight.trackingCode)}</small>`:''}</div><div><span>Total</span><strong>${brl(o.total_cents)}</strong></div><button data-order="${esc(o.id)}" class="ghost-btn">Detalhes</button></article>`).join('')||'<div class="account-empty">Nenhum pedido encontrado para este e-mail.</div>';document.querySelectorAll('[data-order]').forEach(b=>b.onclick=()=>open(b.dataset.order))}async function open(id){const d=await fetch('/api/account/order/'+encodeURIComponent(id),{cache:'no-store'}).then(r=>r.json());if(!d.ok)return alert(d.message||'Erro');let m=document.getElementById('account-modal');if(!m){m=document.createElement('div');m.id='account-modal';m.className='account-modal';document.body.appendChild(m)}const o=d.order;m.innerHTML=`<div class="account-modal-card"><button class="account-modal-close">×</button><h2>${esc(o.order_number)}</h2><p><b>Status:</b> ${esc(status(o.status))}</p>${o.freight?.trackingCode?`<p><b>Rastreio:</b> ${esc(o.freight.trackingCode)} ${o.freight.trackingUrl?`<a href="${esc(o.freight.trackingUrl)}" target="_blank">acompanhar</a>`:''}</p>`:''}<h3>Itens</h3>${(d.items||[]).map(i=>`<div class="account-item"><span>${esc(i.name)} × ${i.quantity}</span><strong>${brl(i.total_cents)}</strong></div>`).join('')}<h3>Nota fiscal</h3>${(d.invoices||[]).map(n=>`<a class="account-doc" href="/api/account/document?orderId=${encodeURIComponent(id)}&key=${encodeURIComponent(n.key)}">${esc(n.name||'Nota fiscal')}</a>`).join('')||'<p>Ainda não anexada.</p>'}</div>`;m.querySelector('.account-modal-close').onclick=()=>m.remove()}document.addEventListener('DOMContentLoaded',()=>setTimeout(load,500))})();
