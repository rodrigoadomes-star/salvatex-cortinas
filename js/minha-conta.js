(function(){const brl=c=>(Number(c||0)/100).toLocaleString('pt-BR',{style:'currency',currency:'BRL'}),esc=s=>String(s||'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m])),labels={aguardando_pagamento:'Aguardando pagamento',pago:'Pago',em_producao:'Em produção',pronto:'Pronto',enviado:'Enviado',entregue:'Entregue',cancelado:'Cancelado',reembolsado:'Reembolsado'};let cfg=null,widgets=new Map();async function turnstileConfig(){return cfg||(cfg=await fetch('/api/turnstile-config',{cache:'no-store'}).then(r=>r.json()).catch(()=>({enabled:false})))}async function renderTurnstile(form){if(!form)return;const c=await turnstileConfig(),slot=form.querySelector('.account-turnstile');if(!c.enabled||!slot||widgets.has(form))return;for(let i=0;i<40&&!window.turnstile;i++)await new Promise(r=>setTimeout(r,100));if(window.turnstile)widgets.set(form,turnstile.render(slot,{sitekey:c.sitekey,theme:'light'}))}function token(form){const id=widgets.get(form);return id!==undefined&&window.turnstile?turnstile.getResponse(id):''}function reset(form){const id=widgets.get(form);if(id!==undefined&&window.turnstile)turnstile.reset(id)}async function submit(form,path,data){const out=form.querySelector('.account-feedback'),button=form.querySelector('[type=submit]');out.textContent='';button.disabled=true;try{const r=await fetch(path,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({...data,turnstileToken:token(form)})}),raw=await r.text();let d;try{d=JSON.parse(raw)}catch{throw new Error('O servidor não conseguiu concluir a operação. Tente novamente.')}if(!r.ok)throw new Error(d.message||'Não foi possível continuar.');location.href='minha-conta.html'}catch(e){out.textContent=e.message;reset(form)}finally{button.disabled=false}}function setupForms(){const login=document.getElementById('email-login-form'),register=document.getElementById('email-register-form');if(login){login.onsubmit=e=>{e.preventDefault();const f=new FormData(login);submit(login,'/api/auth/login',{email:f.get('email'),password:f.get('password')})};renderTurnstile(login)}if(register){register.onsubmit=e=>{e.preventDefault();const f=new FormData(register);if(f.get('password')!==f.get('confirmPassword')){register.querySelector('.account-feedback').textContent='As senhas não coincidem.';return}submit(register,'/api/auth/register',{name:f.get('name'),email:f.get('email'),phone:f.get('phone'),password:f.get('password')})};renderTurnstile(register)}document.querySelectorAll('.password-toggle').forEach(b=>b.onclick=()=>{const i=b.parentElement.querySelector('input');i.type=i.type==='password'?'text':'password';b.setAttribute('aria-label',i.type==='password'?'Mostrar senha':'Ocultar senha')})}async function load(){if(!document.getElementById('account-content'))return;const s=await fetch('/api/auth/session',{cache:'no-store'}).then(r=>r.json());if(!s.authenticated)return;document.getElementById('account-login').hidden=true;document.getElementById('account-content').hidden=false;document.getElementById('account-title').textContent='Meus pedidos';document.getElementById('account-subtitle').textContent='Acompanhe pedidos, rastreio e documentos.';const logout=document.getElementById('account-logout');logout.hidden=false;logout.onclick=async()=>{await fetch('/api/auth/logout',{method:'POST'});location.reload()};document.getElementById('account-user').innerHTML=`<strong>${esc(s.user.name||'')}</strong><span>${esc(s.user.email)}</span>`;const d=await fetch('/api/account/orders',{cache:'no-store'}).then(r=>r.json());document.getElementById('account-orders').innerHTML=(d.orders||[]).map(o=>`<article class="account-order"><div><span>Pedido</span><strong>${esc(o.order_number)}</strong><small>${new Date(o.created_at).toLocaleDateString('pt-BR')}</small></div><div><span>Status</span><strong>${esc(labels[o.status]||o.status)}</strong></div><div><span>Total</span><strong>${brl(o.total_cents)}</strong></div><button data-order="${esc(o.id)}" class="ghost-btn">Detalhes</button></article>`).join('')||'<div class="account-empty">Nenhum pedido encontrado nesta conta.</div>'}document.addEventListener('DOMContentLoaded',()=>{setupForms();setTimeout(load,500)})})();

