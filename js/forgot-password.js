(async function(){const form=document.getElementById('forgot-form'),out=form.querySelector('.account-feedback'),cfg=await fetch('/api/turnstile-config').then(r=>r.json()).catch(()=>({}));for(let i=0;i<40&&!window.turnstile;i++)await new Promise(r=>setTimeout(r,100));let widget=null;if(cfg.sitekey&&window.turnstile)widget=turnstile.render(form.querySelector('.account-turnstile'),{sitekey:cfg.sitekey});form.onsubmit=async e=>{e.preventDefault();const f=new FormData(form),r=await fetch('/api/auth/forgot-password',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email:f.get('email'),turnstileToken:widget===null?'':turnstile.getResponse(widget)})}),d=await r.json();out.textContent=d.message||'Não foi possível continuar.';if(widget!==null)turnstile.reset(widget)}})();

